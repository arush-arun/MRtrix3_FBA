# FBA Pipeline Configuration File

# Paths
INPUT_PATH=/path/to/your/bids/folder
DERIVATIVES_PATH=derivatives
SUBLIST_FILE=./sublist.txt

# BIDS naming convention (currently working for only 1 session)
SESSION=ses-1

# Phase encoding directions
MAIN_PE_DIR=AP
BLIPPED_PE_DIR=PA

# Processing parameters
VOXEL_SIZE=1.25
PHASE_ENCODING_DIR=AP

# Quality control
MIN_SNR=10

# Resume capability
ENABLE_CHECKPOINTS=true
CHECKPOINT_DIR=./fba_checkpoints

# Advanced options
EDDY_OPTIONS="--slm=linear --cnr_maps"
FORCE_OVERWRITE=true
DEBUG_MODE=false
